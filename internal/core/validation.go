package core

import "github.com/the original repository/internal/intent"

func ValidateRecord(newRecordIntent *intent.RecordIntent, existingRecordIntents []*intent.RecordIntent) error {
	return nil
}
